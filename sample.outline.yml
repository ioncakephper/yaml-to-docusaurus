
sidebars:
    - docs:
        autofolder: true
        items:
          - Overview
          - Getting started:
              - Installation
              - CLI
              - Outline basics
          - Outline properties:
              brief: Properties for outline, sidebar, category, topics, and headings.
              headings:
                - Outline file:
                      - sidebars:
                            brief: mention this is a required property and it expects and array. Also mention that each sidebar item is specified in either short or long-hand format. Mention page for both formats.
                      - instance:
                            brief: mention it indicates the Docusaurus instance for which the sidebars are generated. It a purely informative, and designed for future versions.
                - Sidebar properties:
                      - label
                      - items
                      - path
                      - slug
                      - id
                      - autogenerated
                      - autofolder
                - Category properties:
                      - label
                      - items
                      - path
                      - slug
                      - id
                      - title
                      - brief
                      - description
                      - generated_index
                      - doc
                      - order
                          
          - Quick demo
          - CLI
     - tutorials:
          path: tutorials
          autofolder: true
          items:
            - Basic setup:
                title: Set up Skelosaurus and Docusaurus
                brief: Create a Docusaurus instance, install Skelosaurus, and create the documentation outline.
                headings:
                  - Create Docusaurus instance
                  - Install Skelosaurus
                  - Create documentation outline
            - Multiple sidebars in an outline file
            - Several outline files
            - Multi-instance Docusaurus:
                brief: Build sidebar
     
